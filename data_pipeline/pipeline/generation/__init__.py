"""Scenario planning and record-generation commands.

``builder`` owns deterministic scenario contracts and record construction;
``run_subscenario`` is the resumable single-job entrypoint; and
``generate_full_dataset`` orchestrates all configured jobs and release gates.
"""
