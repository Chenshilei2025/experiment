"""Loyal Agent dataset-construction pipeline.

Subpackages follow the release lifecycle:

``generation`` builds scenario-scoped records and private audits;
``validation`` supplies the read-only diversity and quality gates;
``miu`` contains MIU-specific release repair and baseline-audit tools; and
``operations`` maintains inventories, recoverable audits, and quarantines.
"""
